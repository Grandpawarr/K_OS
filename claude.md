# Change Log

## 2026-07-09
- Documented the file-system and IDE code: added Doxygen headers/field
  comments across `code/kernel/src/fs.c`, `code/kernel/inc/fs.h`, and the
  file-system fields of `struct ide_ptn` in `code/kernel/inc/ide.h`. Comments
  only — no behavior change.
